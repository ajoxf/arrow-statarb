# Arrow broker SDK.
#
# 1.7.0 AT MINIMUM, and the reason is one enum value: MCXFO. Every
# release before it carries MCX and only MCX, which is the value for
# permission checks and the instrument download — NOT the one an
# order, a quote or a margin request carries. On an older build MCX
# futures cannot be addressed at all, and the Exchanges page says so
# rather than sending a quote to a value that answers with no book.
pyarrow-client>=1.7.0

# TOTP / auth
pyotp

# Networking / streaming
websocket-client
requests

# Web app
flask
flask-socketio
python-socketio
python-engineio
# Production WSGI server (run_arrow.py --server waitress; recommended over the
# Werkzeug dev server, which can exhaust memory under the dashboard's polling).
waitress

# Config / env / logging
pyyaml
python-dotenv
loguru

# Quant
numpy
pandas

# Optional: exact erfi for the probability filter (a pure-Python fallback is
# used if scipy is absent).
scipy

# --- Arrow Trader (arrowtrader/) ---------------------------------------
# The terminal shares the SDK, TOTP and Flask above. It adds nothing at
# runtime; playwright is test-only and the suite skips cleanly without
# it — a box with no browser runs everything else.
# pip install playwright && python -m playwright install chromium
