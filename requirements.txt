# Arrow broker SDK. >=1.5.0 required from 1 Jul 2026: the HFT data stream
# (wss://socket.arrow.trade) switches to MANDATORY zstd compression; 1.5.0+
# negotiates zstd=1 and decompresses inbound tick frames automatically.
pyarrow-client>=1.5.0

# TOTP / auth
pyotp

# Networking / streaming
websocket-client
requests

# Web app
flask
flask-socketio
python-socketio
python-engineio
# Production WSGI server (run_arrow.py --server waitress; recommended over the
# Werkzeug dev server, which can exhaust memory under the dashboard's polling).
waitress

# Config / env / logging
pyyaml
python-dotenv
loguru

# Quant
numpy
pandas

# Optional: exact erfi for the probability filter (a pure-Python fallback is
# used if scipy is absent).
scipy
