# Arrow broker SDK
pyarrow-client

# TOTP / auth
pyotp

# Networking / streaming
websocket-client
requests

# Web app
flask
flask-socketio
python-socketio
python-engineio
# Production WSGI server (run_arrow.py --server waitress; recommended over the
# Werkzeug dev server, which can exhaust memory under the dashboard's polling).
waitress

# Config / env / logging
pyyaml
python-dotenv
loguru

# Quant
numpy
pandas

# Optional: exact erfi for the probability filter (a pure-Python fallback is
# used if scipy is absent).
scipy
